9-26
注册页面
问题：改变checkbox的默认样式
方法：隐藏checkbox ，增加一个显示标签，使用jq添加点击事件检测checkbox obj.is(":checked")的选中状态，改变显示标签的背景图。


9-27
注册页面
目标: 当输入框内输入内容时自动显示以该文本开头的内容
方法: 使用focus+keyup方法检测输入框,失去焦点时清除keyup方法,在列表覆盖遮罩层,搜索时显示遮罩层,并把匹配的内容节点克隆添加到遮罩层上.

目标:检查用户名的格式,防止用户重复注册
方法:正则表达式验证用户名 , 注册前获取cookie检测用户是否已经注册

9-28 

登录界面

目标:对比用户信息,检测登录,登录成功后新建一个cookie设置当前登录用户.


9-29

首页布局
目标:检测用户是否登录改变用户状态
方法:获取相关cookie,写入用户栏,未登录显示登录注册按钮


10-8


目标:顶部二级菜单 
方法:移入移出触发,设置二级菜单对象, json中读取数据
问题:从json中读取的数据使用json.parse报错
方法:json获取的数据为对象非字符串,无需使用json.parse

目标:banner图二级菜单,纵向布局,动态改变横向长度.
方法:检测获取到数据长度,在超出一定限度后新建ul浮动排列

目标:轮播图
方法:设置轮播图对象,图片与点阵由外界获取  
问题:使用$.proxy函数在循环体中如何将下标传入后续的事件函数
方法:jq on添加事件可以传入事件源 下标可以从data数据中获取
问题:二级菜单没有正确的出现消失
方法:消失事件必须使用mouseleave事件处理

目标:倒计时
方法:每秒获取一次当前时间与目标时间对比

目标:横向滑动的列表模块,每次滑动固定长度,长度不足时滑动剩余长度,当左(右)侧没有内容后,禁用左(右)按钮
方法:设置列表对象,获取列表长度,设置左右长度参数,每次滑动检测剩余长度.

*.首页未完成

10-9

详情页布局

目标:根据传入的id在json中获取相关信息写入页面.
问题:点击事件委托设置时 点击到子节点会将事件源绑定到子节点上
方法:若需要批量处理多个子节点的数据,可以将子节点的z-index设置为-1.

目标:左侧轮播图在滚动一定高度时会跟随移动,一定高度后停止
方法:滚动一定高度时改变父元素的定位top值,一定高度后停止.

目标:选择颜色时会改变左侧轮播图中的图片和图片数量.
方法:点击时更改轮播图对象的图片列表,重新获取列表长度生成点阵.

目标:加入购物车
方法:设置localStorage.cart , 如果不存在写入新key值,如果存在,num属性+1

10-10

购物车布局

目标:显示购物车,没有内容显示空样式
方法:查找localStorage.cart 若为空字符或"{}"则显示空样式,否则将内容写入表中

目标:加减号的事件
方法:执行加减时改变小计,以及localstorage.cart中的num
 
目标:选择事件
方法:设置获取当前选择数量和总价的函数,当商品的选择状态改变时调用函数更新内容(加减号事件中也调用)

10-11

列表页布局

目标:导航栏上的购物车,显示商品数量,单价个数等
方法:写入时检测localStorage获取商品信息写入菜单,移入时显示.



*.使用$(obj).事件名(null);无法清除事件  应该使用 jq中的unbind方法 使用空格分割
删除对象中的键值对用delete1
jq删除dom节点 dom.remove()
